import { StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    dayText: {
      color: 'black',
    },
    temperatureText: {
      color: 'black',
      fontSize: 14,
    },
  });
  
  export default styles;