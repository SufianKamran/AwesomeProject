import { StyleSheet} from 'react-native'

const style = StyleSheet.create({
    dayText: {
      color: 'black',
    },
    input: {
      padding: 15,
      marginTop: 90,
      color: 'black',
    },
  });
  
  export default style;